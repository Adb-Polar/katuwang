Pruned 2026-09-03 — everything from the original list is implemented (see
docs/plans/todo-cleanup-sprint.md, chunks 1-13, plus the Aug 30 follow-up
batch). Only genuinely open items remain below. Each needs a DB/schema
change, so per CLAUDE.md they need explicit DB-modification confirmation
before starting — none are safe pure-UI work.

Open items
 - Tutor appeal button for suspended/banned classes — no code exists yet.
   Needs somewhere to persist the appeal (reason, status, admin review) —
   a schema change, not just UI.
 - Admin topic/subject management page (add/edit topics & subjects) —
   subjects/topics are currently hardcoded in src/lib/subjectTopics.ts.
   Making them admin-editable means moving them into the DB.
 - Community (public) vs. personal (directed) topic requests — this is the
   scope of docs/plans/topic-requests-v2.md ("Not started"). Don't duplicate
   it here; track it in that plan file instead.
