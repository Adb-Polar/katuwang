Learner
 - check if class schedule confict detection exist

Tutor
 [] Edit Class page (/tutor/classes/[classId]/edit) with a SCHEDULED session:
    no "form cannot be a descendant of form" console error on load; the
    Pencil/Reschedule button opens the modal AND IT STAYS OPEN (no flash-close),
    the Topic/Date/Duration form submits and saves, Cancel + ✕ close it.
    Same check for the "Add Session" button/modal. Neither modal submits or
    resets the outer Edit Class form. (Changes.md Part 35)
 [Good] Schedule detection on class creation
 [] Schedule detection on request accept
 [] What happen if u unpublish class with enrolled student
 [] What happen if u suspend or baned classes with enrolled students

fixes.md Phase 1 (Changes.md Part 37)
 [] Any admin list table (/admin/users, /admin/classes): click a sortable
    column header repeatedly -> direction flips asc<->desc every click (not
    stuck after the first click)
 [] /tutor/classes/new: type a topic that already exists in the checklist but
    with different casing (e.g. "algebra" vs "Algebra") -> it's treated as the
    same topic, the catalogue checkbox shows checked, and the "N selected"
    count does not gain a phantom entry
 [] /tutor/classes: an unpublished class card shows only the "Unpublished"
    badge, NOT also a green "Active" badge; a published SCHEDULED class still
    shows "Active"; Completed/Suspended/Banned cards still show their status
 [] Open /tutor/classes/[id]/edit for a COMPLETED class -> redirected to the
    read-only class page
 [] For a SUSPENDED or BANNED class: the tutor class page "Manage Class" menu
    hides "Edit Class Info" and shows a disabled-actions note; the Sessions
    header has no "Manage in Edit" link
 [] Enrolled learner opens a class the tutor has unpublished -> sees the
    "This class isn't published yet" placeholder + "Back to my classes", not
    the full class details

fixes.md Phase 2 (Changes.md Part 38)
 [] Suspended/banned class -> tutor class page -> "Appeal this decision" ->
    the reason textarea shows a live "N/500" counter that turns amber near 500
 [] Verified topics now render as green-outlined pills (with a check) wherever
    they show: class card, class detail page, /learner/tutors/[id] Verified
    Topics; non-verified topics stay neutral/primary outline
 [] /tutor/classes/new and /tutor/requests/[id]/accept: field labels are the
    larger text-sm size; the compact accept-request layout still fits
 [] /tutor/classes: "Class Schedule & Management" header is larger; class
    cards lift slightly + show a primary border on hover
 [] /tutor/classes/new: each session row has a visible grey fill + border
    (doesn't blend into the form background)

fixes.md Phase 3 (Changes.md Part 39)
 [] Char counter shows + updates on: class description (create + edit class),
    topic-request note (learner request form + request-a-specific-tutor),
    session-test instructions; typing stops at the limit and the counter turns
    amber near it; submitting at the limit still saves (no server 400)
 [] Send a few chatbot messages, sign out via /logout -> sign back in ->
    the chat widget starts fresh (no restored transcript)
 [] /tutor/classes/new: for a subject with many topics a "Search topics…"
    box appears above the checklist and filters it; clearing it restores the
    full list; a no-match query shows the hint
 [] /tutor/requests/[id]/accept (few certified topics) -> no search box, or it
    still filters correctly

fixes.md Phase 4a (Changes.md Part 40)
 [] /admin/users: a "Code" column header sorts by anonymous ID (asc/desc,
    re-clickable); combines with search + pagination
 [] /admin/classes: a dedicated "Code" column sorts by class code
 [] /admin/class-appeals: each row has a "View audit log" link -> opens
    /admin/audit-log pre-filtered to that class's id
 [] /tutor/students: the "Enrolled In" column shows class codes (mono
    badges); hovering shows the subject + topics
 [] /tutor/profile: a "Verified Topics" card lists the tutor's certified
    topics grouped by subject (green pills); empty state when none

fixes.md Phase 4b (Changes.md Part 41)
 [] /tutor/requests: three tabs — "Directed to me" (only requests sent to
    this tutor), "Public requests" (open pool, no direct target), "Accepted
    by me"; subject filter + pagination work on each
 [] Tutor global search: typing a subject/topic surfaces a "Class requests"
    group for eligible OPEN requests; clicking a row opens
    /tutor/requests/[id]/accept; a request not open to the tutor doesn't appear
 [] Admin sets "Require certification for class creation" ON -> /tutor/classes/new
    topic list only lets the tutor check topics they're certified for, the
    "add another topic" box is gone, and an explanatory line shows; setting OFF
    restores the full picker
 [] /tutor/assessments: the "Take an assessment for any topic" card lets a
    tutor pick any subject+topic and start the quiz even with no class for it;
    an already-certified topic is rejected; a thin question bank shows the
    BANK_NOT_READY message

fixes.md Phase 4c (Changes.md Part 42)
 [] /tutor dashboard: each of the 4 stat cards shows an icon; numbers still
    correct; "Enrolled Learners" / "Open Topic Requests" cards still link out
 [] /tutor dashboard: "Upcoming sessions" rows show a weekday+date badge,
    subject badge, topic, time+duration; clicking a row opens the class
 [] /tutor dashboard: "This week" is a Mon–Sun grid; sessions land in the
    right weekday column, ordered by time, and link to the class; empty state
    shows when there are no sessions in the next 7 days
 [] /learner/profile and /tutor/profile: the page is read-only (grade,
    section, contact shown as text) with an "Edit details" button
 [] /learner/profile/edit and /tutor/profile/edit: the form loads with
    current values, saves, shows "Profile updated", "Back to profile" works;
    visiting the edit URL logged out redirects to login

fixes.md Phase 5 (Changes.md Part 43)
 [] Every place that said "topic request" now says "class request": learner
    "My class requests" nav + page, tutor "Requests" page + dashboard stat
    ("Open Class Requests"), admin "Class Requests" nav + page title,
    notification page subtitles, chatbot/FAQ/help answers
 [] The feature still works end to end (post a request, tutor accepts, class
    created) — routes and data unaffected by the copy change

fixes.md Phase 6 (Changes.md Part 44)
 [] Admin suspends/bans a tutor's class -> tutor files an appeal from the
    class page -> every admin gets a "TUT-xxxx appealed the moderation on
    <subject> · <code>" notification; the bell dot + sidebar badge update;
    clicking it opens /admin/class-appeals
 [] Filing a second appeal while one is pending still 409s and sends no
    notification

UI fix (Changes.md Part 45)
 [] /tutor dashboard: each of the 4 stat cards shows the label on its own
    line, the number below it, and the tinted icon top-right — not all three
    on one line. Check the learner + admin dashboards look right too.

Seed — session-test charts (Changes.md Part 46)
 [] Run `pnpm exec tsx prisma/seed.ts` then
    `pnpm exec tsx prisma/seed-session-tests.ts` — completes with no error and
    prints the summary.
 [] Chart (a): log in as demo@tutor.test -> /tutor/classes -> demo MATH class
    -> Progress panel shows 5 sessions with pre+post bars, S4 with a pre bar
    only, and no bar/gap for the future session (S6).
 [] Chart (b): log in as demo@learner.test -> /learner/progress -> the area
    chart has a point per session, dips at the S2 regression, and handles the
    S4 null-post gracefully; "Avg gain" stat is non-null.
 [] Chart (c): tutor -> demo class -> session 1 "View results" -> the
    per-question correct-rate chart has 6 columns with differing pre/post bars.
 [] Chart (d): tutor -> demo class -> session 2 results -> DeltaBar shows at
    least one positive, one negative and one zero bar; session 3 results show
    "—" (null) delta rows for learners who skipped the post-test.
 [] Re-run seed-session-tests.ts a second time -> still succeeds, no duplicate
    sessions/tests/questions, roster count unchanged.

Chart fix — pre-vs-post charts, duplicate-label + order (Changes.md Part 47)
 [Good] /learner/progress as demo@learner.test: hovering the 4th point (pre-only
    "Linear Equations & Inequalities") shows "Pre-test: 33%" then
    "Post-test: not taken yet" — verified live in-browser. Each of the 5
    points now shows its OWN row (was collapsing duplicate topic labels).
 [Good] Legend + tooltip list Pre-test before Post-test.
 [] Tutor chart (a) — /tutor/classes -> C-0012 (demo MATH class, has 3
    "Algebraic Expressions" + 2 "Linear Equations" sessions) -> Progress
    panel: each bar group is its own session (duplicates not merged), the
    x-axis still shows readable topic labels, S4 has a pre bar but no post
    bar, and the tooltip labels match the hovered bar.
 [] Tutor chart (c) per-question rate ("Q1".."Q6"): unchanged — labels were
    already unique; confirm nothing regressed.

Dev Chart Lab (Changes.md Part 48)
 [] /dev/charts loads (dev only; 404 in a production build). "Chart Lab ->"
    link on /dev works; "<- Dev Tools" link back works.
 [] Edit a pre/post cell -> all three charts (a/b/d) update live. Blank the
    post cell of a row -> chart (a) drops that post bar, chart (b) shows a
    gap, chart (d) shows "-" for that row. No console errors.
 [] Scenario buttons swap the dataset: "Duplicate labels" -> 3 distinct bars
    (not merged) and NO "two children with the same key" console error;
    "Empty" -> every chart shows its empty hint; "Negative & zero deltas" ->
    DeltaBar shows a down bar, a zero, and up bars.
 [] Per-question editor drives chart (c); single-series editor drives the
    BarChartCard.

Admin (2026-09-03 session)
 [] Question bank drill-down: subject list -> topic list -> questions table;
    breadcrumb "Back" + crumbs navigate back up (Changes.md Part 11)
 [] Assessment pages: /admin/assessment/{question-bank,requests,results} each
    load, sidebar "Assessment" group highlights correctly, old
    /admin/question-bank redirects (Part 11)
 [] /admin/topic-requests: learner / directed-tutor / linked-class cells are
    links to the right detail pages; rows-per-page selector changes page size (Part 11)
 [] Settings -> Assessment: change Questions per attempt / Pass % / Min bank
    size, blur to save, reload to confirm persisted; CoveragePanel in question
    bank shows the new values read-only (Part 15)
 [] Tutor starts an assessment -> served question count + pass mark reflect the
    global settings, BANK_NOT_READY fires below the global min bank size (Part 15)
 [] After dropping topic_assessment_configs (Part 17): app boots, Settings ->
    Assessment loads, question-bank coverage + tutor quiz start still work
    (no lingering prisma.topicAssessmentConfig call)

Auth (2026-09-03 session)
 [] Log in with a PENDING account -> lands on /pending-approval (not an inline
    error); "Back to sign in" returns to /login (Part 14)

Notifications (2026-09-04 session — Changes.md Part 18)
 [] Topbar bell: click opens the dropdown; outside-click, Escape, and navigating
    all close it — check in learner, tutor, and admin portals
 [] Red dot shows on the bell when unread > 0; after clicking a dropdown row (or
    "Mark all read") and the page refreshes, the dot + sidebar badge update
 [] Dropdown row WITH a link -> marks that one read AND navigates; row WITHOUT a
    link -> marks read, no navigation; "View all" opens the notifications page
 [] Notifications page: opening it no longer marks everything read; clicking one
    row marks just that row; "Mark all read" still clears all; sidebar badge +
    bell dot both update without a manual reload
 [] /admin/notifications renders; "Notifications" appears in the admin sidebar
    under "Review" and highlights when active
 [] Admin approves a pending registration -> the applicant sees
    REGISTRATION_APPROVED after logging in; declining sends no notification
 [] Admin certifies / rejects a topic certification -> the tutor is notified,
    link lands on /tutor/assessments
 [] Admin resolves / dismisses a question request -> the requesting tutor is
    notified
 [] Learner enrols in a class -> that class's tutor sees CLASS_ENROLLMENT_NEW
    naming the learner's STU-xxxx; learner unenrols -> CLASS_ENROLLMENT_DROPPED
 [] Tutor completes / cancels a class, or admin bans it -> every enrolled
    learner is notified once; a learner who joined via a topic request gets only
    the TOPIC_REQUEST_* notification (no duplicate CLASS_* one)

Chatbot assistant (2026-09-04 session — Changes.md Part 19)
 [] Floating launcher (bottom-right) appears in learner, tutor AND admin
    portals; click opens the panel with a greeting + suggestion chips; Escape
    or the X closes it; the page still scrolls behind it
 [] A nav question ("how do I enroll", "where are my notifications",
    tutor: "how do I get certified", admin: "approve registrations") returns
    the right answer AND a working deep link
 [] An FAQ question ("is katuwang free", "why can't I see the tutor's real
    name", "can you do my homework") returns the KB answer
 [] A Taglish phrasing ("paano sumali sa klase", "libre ba", "saan ang
    notification ko") resolves to the same intent as its English form
 [] Gibberish ("asdfgh") returns the fallback help with quick-reply chips
 [] Learner only: "recommend a science class" / "I need help with algebra"
    returns up to 3 class cards linking to /learner/classes/[id]; with no open
    class in that subject it returns the "post a topic request" CTA
 [] Send a few messages, reload the page -> the transcript is restored from
    localStorage
 [] /admin/settings -> turn "Chatbot assistant" OFF -> widget disappears on
    reload in every portal and POST /api/chatbot returns 403
 [] After sending gibberish, a chatbot_misses row exists (prisma studio /
    direct query) with the message, role and userId

Registration Approvals filters (Changes.md Part 21)
 [] /admin/registrations: role tabs (All / Learners / Tutors) filter the queue;
    grade-level dropdown filters it; combine with the text search + pagination

Declined-registration state (Changes.md Part 22)
 [] Admin declines a pending registration with a reason -> account status is
    DECLINED (not BANNED); it no longer shows in the Users table unless the
    status filter is set to "Declined"
 [] The declined applicant tries to sign in -> lands on /account-declined with
    the reason shown; "Back to sign in" and "Register again" links work
 [] Declined applicant with NO reason recorded -> /account-declined renders
    without the reason callout
 [] Admin opens a DECLINED user via the "Declined" filter and sets it back to
    ACTIVE -> that person can now sign in

Tutor class appeals (Changes.md Part 23)
 [] Admin suspends/bans a tutor's class -> tutor opens the class edit page,
    sees the appeal card under the moderation panel; "Appeal this decision" ->
    reason textarea (needs >=10 chars) -> submit -> card flips to "awaiting review"
 [] Filing a second appeal while one is pending is blocked (409)
 [] /admin/class-appeals: Pending tab lists the appeal with class + tutor reason;
    Approve -> class returns to SCHEDULED, tutor gets CLASS_APPEAL_APPROVED
    notification, class edit page unlocks
 [] Reject with a note -> appeal moves to Rejected tab, class stays moderated,
    tutor gets CLASS_APPEAL_REJECTED with the note; "Appeal again" button appears
 [] "Class Appeals" nav item shows under the admin Review group and highlights

Sortable admin table headers (Changes.md Part 24)
 [] /admin/users: click "Name" / "Status" headers -> rows reorder, chevron
    shows direction, second click flips it, list stays on page 1
 [] Same for /admin/registrations (Name/Role/Requested), /admin/classes
    (Subject/Status), /admin/topic-requests (Subject/Status), /admin/class-appeals
    (Filed/Reviewed)
 [] Sorting combines with the existing search/filter/pagination controls

Subjects & Topics management (Changes.md Part 25)
 [] /admin/subjects: add a subject (name + SLUG) -> appears in the list;
    add topics under it; reorder + disable toggles work
 [] Rename a topic that's used by a class/question -> the class detail, roster,
    question bank, and certification rows all show the new name (no stale label)
 [] Delete a subject that's in use -> blocked with a 409 message; disable works
 [] Delete an unused topic -> removed; delete a used topic -> "deactivated" toast,
    row shows struck-through
 [] Learner Auto Match / topic request + tutor create-class + assessment /
    certification request dropdowns list the DB subjects/topics (add a new one in
    /admin/subjects, confirm it shows in these forms after a refresh)
 [] Existing seeded data still validates (start an assessment, post a request,
    create a class for a seeded subject/topic) after the enum removal
 [] `grep -r SubjectArea src/` returns nothing; `npx prisma studio` shows
    subjects/topics tables populated and the `subject` columns unchanged

Help & FAQs pages (2026-09-04 session, Changes.md Part 26)
 [] /learner/help, /tutor/help, /admin/help each load; sidebar "Help & FAQs"
    item highlights; a learner hitting /admin/help is redirected (role guard)
 [] Topbar "?" button navigates to the current portal's /help page
 [] FAQ search box filters the accordions across question/answer/category and
    auto-expands matches; "No FAQs match" shows for a nonsense query
 [] "Jump to" quick links and guide content point to real pages for that role

Chatbot FAQ role-scoping (2026-09-04 session, Changes.md Part 27)
 [] As a learner, ask the chatbot "how do I become a tutor" / "what happens
    after I pass an assessment" -> gets the role fallback, NOT the tutor answer
 [] As a tutor or admin, ask "how does matching work" / "my class was cancelled"
    / "one-on-one vs group" -> role fallback, NOT the learner answer
 [] As every role, ask "is Katuwang free" / "why can't I see real names" ->
    still answered (platform-wide FAQs unaffected)

Global search + Find Tutors (Changes.md Part 28)
 [] Top-bar search: type 2+ chars -> a dropdown appears with grouped results
    (Classes / Tutors / Topics); it closes on outside-click and Escape
 [] Cmd/Ctrl+K and "/" (when not already typing) focus the search box
 [] Arrow up/down moves the highlight, Enter opens the highlighted result,
    clicking a row navigates and clears the box
 [] Learner search: class rows go to /learner/classes/[id], tutor rows to
    /learner/tutors/[id], topic rows pre-filter /learner/classes
 [] Tutor search: only their OWN classes appear; no Tutors/Accounts group
 [] Admin search: all classes + non-admin accounts; account rows open
    /admin/users/[id]
 [] Search with a gibberish query -> "No matches for ..." (no crash)
 [] /learner/tutors: card grid of verified tutors with subjects, verified-topic
    count, class count, next session; filter by ID text and by subject
 [] Clicking a tutor card opens /learner/tutors/[id] (existing profile page)
 [] With showTutorRealNames OFF the cards show no real names; turning it ON in
    /admin/settings makes the name appear on the card
 [] "Find Tutors" appears in the learner sidebar under Main menu and highlights
    when active

Session pre/post-tests - Phase 0: chart extraction (Changes.md Part 29)
 [] /admin/reports still renders every chart exactly as before (enrollments
    area chart + the 6 bar-chart cards); "Show data table" toggles still work
 [] No visual/console regression on that page after the recharts extraction

Session pre/post-tests - Phase 2: origin isolation (Changes.md Part 29)
 [] Admin -> Assessment -> Question Bank: add + edit a question still works
    (shared QuestionFormModal); the modal shows "<subject> . <topic>" context
 [] The admin question-bank list/count still shows only admin-authored questions
 [] Certification quiz (tutor): on a topic with a near-empty admin bank, the
    tutor still gets BANK_NOT_READY even after authoring custom questions
 [] Certification quiz still serves bank-only questions (no tutor-authored ones)

Session pre/post-tests - Phases 3-4: backend routes (Changes.md Part 29)
NOTE: the exact status codes/error codes below are easiest to check via
curl/Postman against the seeded demo class+session; the Phase 6 block covers
the same flows through the actual screens.
 [] Learner: POST .../test/PRE/start while the session is SCHEDULED succeeds;
    the same call while COMPLETED 409s with code PRE_WINDOW_CLOSED; POST/start
    while SCHEDULED 409s with code POST_NOT_OPEN
 [] Tutor marks the session COMPLETED (with a PUBLISHED test) -> learners get
    SESSION_POSTTEST_OPEN; learner can now start the POST test; submit ->
    receives score + delta only once BOTH pre and post are submitted
 [] A learner who already had the pre-test open when the tutor hit "Mark
    Complete" can still resume and submit that in-progress PRE attempt
 [] GET /api/admin/session-tests/[id]/results shows per-question rates +
    per-learner deltas with no `id` field on any learner row, only
    `anonymousId`
 [] GET /api/learner/progress returns only this learner's own series - no
    tutor name, no class-average figure anywhere in the payload

Session pre/post-tests - Phase 5: tutor UI (Changes.md Part 29)
 [] Tutor class page: a session with no test shows "Build test" in the new
    Session Tests card; clicking it lands on the builder with the create-test
    form (title/instructions only)
 [] Builder: create a test, add questions from the bank, add a self-authored
    one via "Write a question", reorder + remove, "Save question set", then
    Publish -> question list becomes read-only, enrolled learners get
    SESSION_PRETEST_OPEN, "View results" link appears
 [] Builder on a PUBLISHED test: "Close test" works and flips the badge; on a
    test with >=1 attempt, editing controls (add/reorder/remove/save) are
    gone even while still DRAFT
 [] Session Tests card on the class page reflects Draft/Open/Closed status
    per session and its Build/View/Results links go to the right session
 [] Results page: per-question RateBarChart (pre vs post bars) and
    per-learner DeltaBar render correctly with 0, 1, and several submitted
    attempts; "no data" states show the empty-hint text, not a broken chart
 [] Results page per-learner table: "Pre"/"Post" buttons each open the
    correct attempt's detail modal (right answers highlighted, picked answer
    marked) and only appear when that attempt exists
 [] Class page: ClassProgressPanel (chart a) shows one pre/post bar-pair per
    session with a test, a gap (not a 0% bar) for sessions with no test yet,
    and the class-wide avg-gain footer text only when pairedCount > 0
 [] Turn "sessionTestsEnabled" OFF via Prisma Studio (platform_settings) ->
    the builder's create-test POST 403s with a clear error banner; existing
    results/builder pages for already-created tests stay reachable

Session pre/post-tests - Phase 6: learner + admin UI (Changes.md Part 29)
 [] Learner class page: Session Tests card shows "Take pre-test" once
    published; taking it and submitting shows the score banner (no correct
    answers revealed on an in-progress attempt if you refresh mid-test)
 [] After the tutor marks the session COMPLETED: card now shows "Take
    post-test" for a learner who already submitted the pre-test; submitting
    the post-test shows the score AND the delta vs the pre-test score in the
    banner text
 [] A learner who never took the pre-test still sees "Review pre-test" (not
    "Take post-test") once the post window opens, since there's nothing
    paired to show — reflects the seeded juan.delacruz row (pre only)
 [] Resuming an in-progress pre-test (leave mid-quiz, come back) restores
    previously-selected answers and does not reveal correct/incorrect yet
 [] Reviewing an already-submitted attempt (either kind) shows the
    correct/incorrect marks, the explanation text, and does not let you
    change your answer
 [] /learner/progress: ProgressAreaChart renders the seeded demo@learner.test
    gain; the class filter narrows the series correctly; summary numbers
    (sessions with test, paired count, avg gain) match the chart
 [] Compact MyProgressView embedded on the learner's class page has no class
    filter dropdown and matches the same data as the full page filtered to
    that class
 [] /admin/session-tests: subject/status/text filters and column sorting
    (SortableTh) all work against the seeded data; "Results" opens the same
    results view the tutor sees, still double-blind (no learner id visible)
 [] /admin/settings -> Assessment group shows "Session pre/post-tests" toggle;
    flipping it off/on matches the platform_settings row and the tutor/learner
    403 behavior above
 [] Both nav items highlight correctly when active: "My Progress" (learner,
    Main menu) and "Session Tests" (admin, Assessment group)

Session pre/post-tests - Phase 7: seed data (Changes.md Part 29)
 [] Log in as demo@tutor.test -> the "Algebraic Expressions" session under
    the demo MATH class is COMPLETED and has a PUBLISHED session test with 6
    questions (5 bank + 1 custom); its results page shows demo@learner.test's
    pre (~40%) -> post (~85%) gain and juan.delacruz's pre-only (~60%) row
 [] Log in as demo@learner.test -> /learner/progress shows this session with
    a visible gain; the class's Session Tests card shows both attempts as
    "Review"
 [] Re-running `pnpm exec tsx prisma/seed.ts` a second time is clean (no FK
    errors) and reproduces the same seeded session-test state each time

Chatbot v1.1 - usability pass (Changes.md Part 30)
 [] /admin/chatbot renders; role filter + text search + sort by count/first
    seen/last seen + pagination all work; "Chatbot" nav item highlights when active
 [] Ask the assistant gibberish ("asdf qwerty zxcv") as each role -> after a
    refresh a row appears in /admin/chatbot for that role
 [] Two near-identical gibberish messages differing only in case/punctuation
    (e.g. "Foo Bar?" then "foo bar") -> collapse into one group with count 2
 [] Ask "help" alone -> no longer returns the capabilities blurb; ask "what can
    you do" -> still returns it
 [] Misspelled queries ("how do i enrol", "notificaton") -> correct page /
    answer still suggested
 [] New nav intents deep-link correctly: "find tutors" (learner), "help page"
    (all 3 roles, correct /{role}/help), "appeal a suspended class" (tutor),
    "audit log" / "subjects" / "chatbot" (admin)
 [] New FAQ entries retrieve correctly for a few phrasings (e.g. "why can't I
    sign in after registering", "is there a class capacity limit")
 [] Chatbot still answers its existing pre-v1.1 canned topics unchanged (e.g.
    "how do I enroll", "is katuwang free")

Sept 5 fixes - 9-item batch (Changes.md Part 31)
 [] /learner/match: run Auto Match -> the panel swaps from the criteria form to
    the ranked results with a short, subtle fade (not a tilted 3D spin);
    "Adjust filters" swaps back; once results exist a "View results" button on
    the form swaps forward again; motion is suppressed with reduce-motion on
 [] /register/learner and /register/tutor: eye toggle reveals/hides each
    password field independently; submit still validates match + min length
 [] /learner/profile: new two-column layout (Identity read-only / Editable
    details); grade level is a working <select>; saving persists it
 [] Contact info validation: "0917 123 4567", "+63 917 123 4567" save and
    display as 09171234567; "123" or a US number is rejected with the PH-mobile
    message; a Messenger handle ("Tita Rosa on FB") saves as-is
 [] /tutor/profile: grade level + section both editable and persist; contact
    info validated the same way
 [] Global search as a learner while admin setting "Show tutor real names" is
    ON: searching a tutor's real name finds them and the row shows the name
    (anon ID beneath); with the setting OFF, only anon-ID search works and only
    the anon ID shows
 [] /tutor/assessments: click "Request questions" on a topic -> switch to
    "Assessment History" tab -> switch back -> the topic still shows "Questions
    requested" (no revert, no refresh needed)
 [] After that request, log in as an admin -> /admin/notifications shows a new
    "requested assessment questions for ..." entry linking to
    /admin/assessment/requests; the topbar bell unread dot updates
 [] /admin/classes and /admin/topic-requests: the row action buttons
    (View/Suspend/Ban/Reinstate, Close/Re-open) sit correctly aligned at the
    right of each row, wrapping instead of overflowing on a narrow window
 [] /tutor/classes -> "Schedule a Class" -> pick subject + topic(s) -> the
    Sessions "Add Session" rows: on a narrow / mobile viewport each row wraps
    to two lines (topic + remove, then date/time + duration) and the modal does
    not scroll sideways; on desktop it still reads as a compact row. Same check
    when accepting a topic request (AcceptRequestModal uses the same fields).
 [] /tutor/classes -> "Schedule Class" now navigates to /tutor/classes/new (a
    full page, not a modal); fill it in and submit -> lands on the new class's
    detail page and the class appears in the Active list. "Cancel" returns to
    /tutor/classes. The empty-state "Schedule your first class now" link also
    goes to /tutor/classes/new. Direct-visiting /tutor/classes/new while logged
    out redirects to login (proxy /tutor guard).
 [] On /tutor/classes/new the inputs/selects are full-size (input-md /
    select-sm, text-sm) and comfortably spaced; the accept-a-topic-request
    dialog still uses the compact control sizes.
 [] /tutor/requests -> "Accept & create class" now navigates to
    /tutor/requests/[id]/accept (a full page, not a modal); subject is locked,
    only certified topics selectable, first session pre-filled from the
    request's earliest preferred slot; submit -> lands on the new class detail
    page. "Cancel" returns to /tutor/requests. Visiting the accept URL for a
    request NOT open to you (or when matching is disabled) 404s.

Session pre/post-tests - Phase 8: learner review + RSC fix (Changes.md Part 29 Phase 8)
 [] Tutor: open a published session test's "Results" (from the class page
    Session Tests card or /tutor/classes/.../test/results) -> page renders, no
    "Functions cannot be passed to Client Components" console error; clicking a
    learner's "Pre"/"Post" opens the per-attempt detail modal.
 [] Learner who has submitted BOTH pre and post: the class page Session Tests
    card row shows "Review pre" AND "Review post" (not just one); each opens the
    correct submitted attempt in review mode (answers + correct + explanations,
    post shows the delta vs pre).
 [] Learner who submitted only the pre-test: row shows "Review pre" plus the
    next action ("Take post-test" when the session is completed, otherwise just
    the review link).
 [] After the tutor CLOSES the test, a learner who took it can still open
    "Review pre" / "Review post"; one who never took it shows "Closed — not taken".
 [] /admin/session-tests -> open a test's "Results": there is exactly ONE back
    control ("← Back to session tests"), which returns to the table without a
    full reload. The tutor page /tutor/classes/.../test/results still has its
    own "Back" link.
 [] /learner/tutors -> the tutor cards read as distinct raised tiles against
    the panel (subtle grey fill + border + shadow), not flush with the
    background; hovering one lifts it, brightens to white, and highlights the
    border.

Admin IA - Certifications moved under Assessment (Changes.md Part 32)
 [] Admin sidebar: "Certifications" now appears in the Assessment group (after
    Results), not in Review; it highlights when active.
 [] Visiting /admin/certifications redirects to /admin/assessment/certifications;
    the review table (Pending/Certified/Rejected tabs, approve/reject) works
    the same as before.
 [] Admin dashboard "Pending certifications" stat card, the admin Help Center
    certifications link, and the chatbot "review certifications" answer all
    deep-link to /admin/assessment/certifications.

Class appeal moved to class detail page (Changes.md Part 33)
 [] Admin suspends or bans a tutor's class -> the tutor opens
    /tutor/classes/[id] (the normal class page, not Edit) and sees the
    moderation panel WITH the "Appeal this decision" card right below it;
    filing/awaiting/rejected states behave as before.
 [] For a SCHEDULED/COMPLETED/CANCELLED class the appeal card does NOT show on
    the detail page.
 [] The edit page still shows the moderation panel for a locked class, now with
    a "go back to the class page" link instead of the appeal form.

M01 group presentation deck (Changes.md Part 49)
 [] Open docs/to-submit/m01-presentation/Katuwang_M01_System-Design-Refinement.pptx
    in PowerPoint / Google Slides -> all 16 slides render, no overflow text,
    speaker notes present on slides 1-16.
 [] Architecture (slide 4), Context DFD (slide 10) and Level 1 DFD (slide 11)
    connectors read cleanly after opening in the real editor; nudge shapes if
    LibreOffice-style connector routing looks crossed.
 [] Replace R2 / R3 placeholder rows (slides 3 and 14) with the team's actual
    Capstone 1 panel recommendations, or delete them if the panel gave only
    the tutor-certification recommendation.
 [] Add the 3 UI screenshots (tutor "not certified" gate, certification
    PENDING state, learner class-browser empty state) into slide 9 from the
    running app (pnpm dev + seed accounts); save copies under assets/ui/.
 [] Fill the Group Identification details on slide 1 (group number + member
    names) and confirm the speaker map on slide 16 matches who will actually
    present.
 [] Rehearse against a timer -> core content lands in 10-13 min leaving room
    for 2-3 min Q&A; every one of the 5 members speaks for at least one section.
 [] DFDs: open assets/DFD.md (GitHub / VS Code mermaid preview) -> both Level 0
    and Level 1 diagrams render; cross-check every DS1-DS12 store against a real
    model in prisma/schema.prisma and every process against a route group.
 [] Deck slides 10 (Level 0 image), 11 (8-process map), 17-18 (DFD appendices)
    render correctly in PowerPoint / Google Slides.

Learner portal fixes (Changes.md Part 52)
 [] Topbar avatar: click it in the learner portal -> /learner/profile. Same in
    the tutor portal -> /tutor/profile. Admin avatar stays non-clickable.
 [] Progress chart (enrolled class -> class page, or /learner/progress): a
    session with a PRE attempt but no submitted POST shows "Pre-test <n>%" then
    "Post-test not taken yet" on hover; legend + hover list Pre-test before
    Post-test. Cross-check /dev/charts "Typical (one unpaired)" for GroupedBarChart
    -> missing bar's hover reads "—", not a number.
 [] Learner dashboard (/learner): stat cards have a tinted icon tile; upcoming
    sessions are bordered tiles with a weekday/day chip; a "This week" Mon-Sun
    grid shows, chips open /learner/classes/<id>.
 [] Browse Classes (/learner/classes): search "Mathematics" (display name, not
    "MATH") returns math classes.
 [] Global search (Cmd/Ctrl+K): scope "Class code" + partial code -> only class
    hits; "Tutor code" + "TUT" -> only tutors; "Subject" + a subject name ->
    subject/class + topic hits; "Everything" behaves as before.
 [] Find Tutors (/learner/tutors): cards show green verified-class code badges;
    toggling Math + Science chips -> only tutors CERTIFIED in both; "Clear"
    resets. Open a profile -> schedule is the weekly-timetable grid, chips open
    /learner/classes/<id>.
 [] Tutor profile as a NON-enrolled learner: the tutor's suspended/banned and
    completed classes are absent; no "Completed" tab. As an ENROLLED learner of a
    completed class: "Completed" tab present; opening that class renders (no 404).
 [] My Classes (/learner/my-classes) -> open a class -> in-page "Back" -> returns
    to /learner/my-classes (not /learner/classes). From Browse it returns to
    /learner/classes.
 [] My Requests (/learner/requests): click "New request" -> existing request
    cards + the "New request" button disappear; "Back to requests" restores them.
    Click "Edit" on an OPEN request -> same; "Save changes" / "Back to requests"
    restores the list.
 [] Profile edit (/learner/profile/edit): type "0917123" in Contact info -> inline
    error appears immediately (before submit); "09171234567" clears it; a
    non-numeric handle shows no error while typing; Save succeeds.
 [] Auto Match (/learner/match): run a match -> each result card shows a
    "Match <n>" badge and the list is ordered highest-first.

Admin portal fixes (Changes.md Part 53)
 [] Sidebar (any portal): only the "essential" groups show their items on first load
    (admin: Main menu + Review; tutor: Main menu + Teaching; learner: Main menu + Tools);
    other group titles show a collapsed chevron. Click a collapsed group -> expands,
    chevron rotates. Navigate into a collapsed group's page -> it auto-expands and its
    toggle is disabled while active. Reload -> manual expand/collapse choices persist.
    Mobile drawer mirrors the same state.
 [] /admin dashboard: the 4 top cards have a tinted icon tile and match the tutor
    dashboard look. In "Needs attention", a count of 0 shows a grey badge; a queue with
    items (pending registration, open appeal, etc.) shows an amber badge; flagged
    accounts > 0 shows a red badge. "Active classes" stays grey regardless.
 [] /admin/users: the new grade-level dropdown filters the list; combine with the
    role tab + status filter; "All Grade Levels" restores it.
 [] /admin/registrations: the "ID" column header is a sort button; toggling it orders
    rows by anonymous ID asc/desc.
 [] /admin/classes: the "Scheduled" column shows a real date for classes with a session
    and "—" for classes with none — never "Invalid Date". A "Rows per page" selector is
    present; changing it to 25/50 reloads with that many rows and updates the URL.
 [] /admin/reports: "Export CSV" downloads katuwang-report-YYYY-MM-DD.csv; open it ->
    header `section,label,count`, one row per breakdown entry, `enrollments_total` rows,
    and 30 `enrollments_by_day` rows. Numbers match the on-page charts.
 [] /admin/subjects: open a subject row's "⋯" menu near the bottom of the scrolled list
    -> the menu is fully visible over the card edge, not clipped. Same for a topic row.
    Menu closes on outside-click / Escape / scrolling the list; move up/down, disable,
    and delete (-> confirm dialog) all still work.

Minimalist scrollbars (Changes.md Part 54)
 [] Scrollable areas (page body, sidebar, wide tables, modals, code blocks) show a
    thin rounded grey thumb with no visible track in Chrome/Edge and Firefox;
    thumb darkens slightly on hover. No layout shift where scrollbars appear.

Learner reports for tutors & classes (Changes.md Part 55)
 [] /learner/tutors/[id] for a tutor whose class you have NOT joined: no "Report this
    tutor" button in the header. Enroll in one of that tutor's classes, reload -> the
    button appears next to "Request a topic from this tutor".
 [] Report modal (tutor): ticking only checklist items submits; ticking "Other" reveals
    a textarea and the submit button stays disabled until 10+ chars are typed. On success
    the button is replaced by "Report sent to the admins."
 [] File a second report about the same tutor while the first is still pending -> inline
    error "You already have a report about this tutor awaiting review." (409).
 [] /learner/classes/[id] while enrolled: a "Report this class" button shows in the
    actions area with the class checklist; not shown when not enrolled.
 [] /learner/reports lists every report you filed with a status badge
    (PENDING amber / RESOLVED green / DISMISSED grey), the ticked reasons, and the admin
    note once reviewed.
 [] /admin/abuse-reports: "Abuse Reports" appears in the sidebar "Review" group after
    "Class Appeals". Pending tab lists new reports; the reporter shows only as STU-XXXX
    (never a real name), tutor target as TUT-XXXX, class target links to /admin/classes/[id].
 [] /admin/abuse-reports: the "All targets / Tutor reports / Class reports" dropdown next
    to the status tabs filters the list to that target type; combine it with each status
    tab; "All targets" restores the full list; the list resets to page 1 on change.
 [] Admin "Resolve" (confirm dialog) and "Dismiss" (optional-note modal) move the report
    to the matching tab, write a row to /admin/audit-log (REPORT_RESOLVED / REPORT_DISMISSED,
    searchable by the report id), and send the reporter a REPORT_REVIEWED notification
    linking to /learner/reports.
 [] Reviewing an already-reviewed report (e.g. via a stale tab) returns "This report has
    already been reviewed." (400).
 [] No real name or email of the reporter or the reported tutor appears anywhere in the
    learner flow, the admin queue, or any notification message.

Clean-code cleanup backlog (Changes.md Part 56) — behaviour-preserving, spot-check only
 [] Dates render consistently after the datetime.ts consolidation. Eyeball a few pages
    that used to have their own format: /admin (recent audit rows), /admin/audit-log,
    /admin/users/[id] + /admin/classes/[id] (Joined / Enrolled / suspended-until),
    /admin/abuse-reports, /admin/registrations, /admin/users (suspension "Until"),
    /tutor (status banner + "Upcoming sessions" tile day/time), /tutor/students/[id],
    /tutor/assessments (AssessmentHistory), /learner (upcoming-session tile),
    /learner/reports, /learner/requests (fulfilled-class "next ..."), a class card's
    "next session", a class detail Sessions list, the weekly timetable time labels.
    Each should show a valid localised date/time (no "Invalid Date", no raw ISO).
 [] Learner/tutor/admin top-bar search: a topic hit still links into the right portal's
    /classes?q=... ( /learner, /tutor, /admin respectively).
 [] Chatbot: ask "where are my notifications" and "open help" as each role — the reply's
    link points at /{portal}/notifications and /{portal}/help for that role
    (admin help -> /admin/help, admin profile question still -> /admin/settings).
 [] Pagination unchanged: admin tables still page at 10, class/tutor browse at 12,
    audit-log + chatbot-misses at 25, dev login board at 8; the rows-per-page selector
    still works where present.
 [] Auto-match / browse ordering unchanged: run /learner/match and the browse list and
    confirm results and scores look the same as before (grade weighting still 6/3/2).

Security-review fixes (Changes.md Part 57)
 [] Login throttle: from one browser/IP, submit ~11 wrong-password logins for the same
    email within 10 min -> the 11th shows "Too many sign-in attempts. Please wait a few
    minutes and try again." A different email from the same IP still works.
 [] Login error is generic: a nonexistent email and a real email + wrong password both
    show "Invalid email or password." (no "no account" vs "incorrect password" tell).
    Response time for the two is visually similar (no fast-fail on unknown email).
 [] Suspended/banned/pending/declined accounts still get their specific screens
    (/pending-approval, /account-declined, inline suspended/banned message).
 [] Forgot-password throttle: 6 requests for any address from one IP within 15 min ->
    429 neutral copy; still no signal about whether the address exists.
 [] /api/dev on a local (non-production) dev server: hit GET /api/dev while logged OUT
    or as a learner/tutor -> 404 {"error":"Not found."}. As an ADMIN -> normal payload.
    The Dev Data Factory UI still works when signed in as admin.
 [] Security headers: load any page, check response headers include
    Content-Security-Policy, Strict-Transport-Security, X-Frame-Options: DENY,
    X-Content-Type-Options: nosniff, Referrer-Policy, Permissions-Policy. App renders
    with no CSP console errors (charts, icons, fonts, client nav all still work).
 [] allowedDevOrigins still honoured (LAN IP 192.168.219.123 can load the dev server
    without the Next cross-origin warning).
 [] Mid-session ban: log in as a learner in browser A; as admin in browser B, ban that
    learner. Within ~5 min (or after the learner's tab refetches the session / navigates)
    browser A is bounced to /login?reason=account-inactive on the next /learner navigation.
    Un-ban -> access returns.
 [] Password policy: registration + reset-password reject "password123" ("too common"),
    reject a 73+ character password, and still accept a normal 8+ char password.
 [] Rate limiting is off under `pnpm test` (suite stays green) and can be disabled locally
    with RATE_LIMIT_DISABLED=1 if it gets in the way during manual QA.

--- Part 58 (design-review D1/D2/D3) ---
 [] Weight contrast: page H1s (PageHeader) and card titles (.kt-card-head h2) now
    render in the heavy 700 Fett face; body emphasis / table headers / badges / buttons
    stay at 500. Nothing small-text looks heavier than before.
 [] font-serif sweep: no visual change anywhere (alias pointed at Apfel already) —
    spot-check auth screens, /register, /dev/*, admin moderation tables, MyProgressView,
    SessionTestResults still render their headings in Apfel.
 [] Fragment Mono nav + buttons: sidebar nav labels and every .btn now render in
    Fragment Mono. Check the nav column doesn't wrap at the default width and button
    labels aren't clipped (Approve/Reject pairs, modal action rows, PageHeader actions).

--- Part 59 (design-review D5/D7) ---
 [] Contrast: run --kt-tutor-text (on the tutor tint), --kt-muted and --kt-faint
    (on base-100) through a real contrast checker at text-2xs (0.6875rem). Target 4.5:1.
    Darken further if any misses. Confirm muted-vs-faint hierarchy still reads (they're
    close now).
 [] Visual sweep: tutor ID badges / avatars / --kt-badge--tutor are a touch darker gold
    but still clearly the tutor colour; secondary text and micro uppercase labels are
    a little darker everywhere — no layout shift.

--- Part 60 (repeated-markup C2: BackLink) ---
 [] Back links still work + look right on: /admin/users/[id], /tutor/students/[id],
    /learner/tutors/[id], class details view, edit-class page, session test results,
    profile edit page. ProfileEditView's back link is now a ghost button (was inline text).

--- Part 61 (repeated-markup C3: gradeLabel/gradeSection) ---
 [] Grade + section still renders as "GRADE 10 · Rizal" on: admin user detail, admin
    class detail roster, tutor student profile, tutor roster + its grade filter, admin
    Users + Registrations tables, learner topic-request cards, tutor topic-request browser,
    ClassCard, enrolled-learners table, profile view.

--- Part 62 (repeated-markup C1: StatCard) ---
 [] Dashboard stat tiles unchanged on all 3 portals: admin (4 plain), learner (3 linked),
    tutor (2 plain + 2 linked). Linked ones still navigate + show the hover border.

--- Part 63 (repeated-markup C4: SearchInput) ---
 [] Learner "Browse classes" search still filters by code/subject/topic; "Find tutors"
    search still upper-cases as you type and matches TUT-#### IDs. Pill looks unchanged.

--- Part 64 (component-reuse R8) ---
 [] Admin Abuse Reports + Class Appeals: violation labels still render; "View audit log"
    link still opens the audit log filtered to the right id. Learner "My Reports"
    violation chips unchanged.

--- Part 65 (component-reuse R4: LoadingRow/EmptyState) ---
 [] Every admin table + Question Bank tabs: loading spinner and "nothing here" empty row
    still appear (and look unchanged) on slow network / empty filters. Also check
    ClassBrowser, tutor ClassManagement, PlatformSettingsForm, ReportsView, SessionTestResults.

--- Part 66 (component-reuse R6) ---
 [] Admin Audit Log: sorting by Action / Target / When still works, direction toggles,
    inactive columns now show a faint up/down chevron. Chatbot Misses: Count / First seen /
    Last seen sort the same, headers now match the rest of the portal.

--- Part 67 (component-reuse R2: PromptDialog) ---
 [] Admin Class Appeals: reject an appeal with + without a note -> tutor notified, note
    stored; char counter shows; cancel/backdrop closes.
 [] Admin Abuse Reports: Resolve now shows an optional note field (new) -> resolves,
    reporter notified. Dismiss with/without note still works.
 [] Admin Certifications: reject a request with feedback -> tutor sees outcome; note saved.
 [] Re-opening any of these dialogs starts with an empty textarea (no stale note).

--- Part 68 (component-reuse R3: Modal) ---
 [] Every ConfirmDialog + PromptDialog (delete confirms, moderation prompts, appeal/
    certification reject, etc.): Escape closes it; Tab cycles within the dialog and
    does not reach the page behind; on close, focus returns to the button that opened it.
    Visuals unchanged (no new X button on these two).
 [] Backdrop click still cancels; while a submit is in flight the buttons are disabled.
