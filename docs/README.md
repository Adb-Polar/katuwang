# Katuwang Docs — Index & Previews

Every document under `docs/` lives in a subfolder; this file is the only page at
the root and previews all of them. Reorganised 2026-09-10 (see `Changes.md`
Part 69) — merged the six review docs and three assessment plans, archived
completed/superseded plans, and moved every loose file into a folder.

```
docs/
├── README.md            you are here — preview index
├── reference/           standing reference (how things are)
├── roles/               per-role feature + API reference
├── plans/               active implementation plans
├── reviews/             consolidated code & design reviews
├── backlog/             flat TODO / manual-test scratchpads
├── reports/             current sprint / feature-status spreadsheet
├── to-submit/           course module deliverables
└── archive/             completed plans, superseded fragments, old reports
```

> `prisma/schema.prisma` is the source of truth for the data model;
> `reference/erd.md` is its rendered view, regenerated by `prisma-erd-generator`
> on `npx prisma generate`.

---

## `reference/` — standing reference

| File | Preview |
|---|---|
| [`project-overview.md`](reference/project-overview.md) | **Start here.** Condensed domain/business context for agents — what Katuwang is, TRIS background, the three roles, the RA 10173 double-blind mandate, the six modules, domain vocabulary. Digest of `thesis.md`. |
| [`decisions.md`](reference/decisions.md) | Authoritative record of where the running app deliberately diverges from the thesis proposal (3 roles not 4, live palette supersedes `theme.md`, Lucide supersedes the no-SVG rule, …). Wins over the thesis text on conflict. |
| [`feature-checklist.md`](reference/feature-checklist.md) | Thesis spec vs. actual implementation status, module by module. Legend: ✅ implemented · ⚠️ partial · ❌ not implemented. |
| [`erd.md`](reference/erd.md) | Auto-generated Prisma ERD (mermaid) for the current schema. Do not hand-edit — `npx prisma generate` overwrites it. Raw mermaid source alongside in [`erd.mmd`](reference/erd.mmd). |
| [`auth-implementation.md`](reference/auth-implementation.md) | Full registration & login build guide (NextAuth 4 credentials + JWT, Prisma, bcrypt). Schema snippets predate later migrations; the architecture is still current. |
| [`chatbot.md`](reference/chatbot.md) | Chatbot Assistant architecture & workflow — a deterministic intent/FAQ responder, no LLM. Now includes the original implementation plan as an appendix. |
| [`theme.md`](reference/theme.md) | **Retired 2026-09-10** to a pointer at `src/app/globals.css` — it had drifted to an older palette. Kept so old links don't 404. |
| [`thesis.md`](reference/thesis.md) | The full ~450KB capstone thesis ("A web-based Peer Tutoring Management Platform … for Taysan Resettlement Integrated School"). A historical proposal snapshot — **do not read wholesale**; use `grep`/offset reads, and prefer `project-overview.md`. |

Non-markdown here: `architecture-design.html` / `.pdf` (rendered architecture diagram).

## `roles/` — per-role reference

| File | Preview |
|---|---|
| [`LEARNER.md`](roles/LEARNER.md) | `STUDENT_LEARNER` role: browse and join peer tutoring classes via `/learner`. Feature list + every `/api/classes/**` endpoint. Enrollment is whole-class. |
| [`TUTOR.md`](roles/TUTOR.md) | `STUDENT_TUTOR` role: offer tutoring via `/tutor`. `TutorClass` as a multi-session course container; certification flow; full `/api/tutor/**` reference. |
| [`ADMIN.md`](roles/ADMIN.md) | `ADMIN` role: moderate accounts/classes, review certifications, configure `PlatformSetting` flags, view reports, via `/admin`. Full `/api/admin/**` reference. |

## `plans/` — active implementation plans

See [`plans/README.md`](plans/README.md) for the status table.

| File | Preview |
|---|---|
| [`assessments.md`](plans/assessments.md) | **Merged.** Admin question bank + auto-graded topic quizzes, per-topic config → one global config, and learner per-session pre/post-tests (`SessionTest` per `ClassSession`). Mostly shipped (Changes.md Parts 29–33). |
| [`deployment.md`](plans/deployment.md) | Free-tier path from local dev to a public URL: Oracle Cloud **or** Render hosting, TiDB Cloud Serverless MySQL, free `.tech`/`.me` domain. Planning only. |
| [`fixes.md`](plans/fixes.md) | Status audit of the ~55-item fixes/redesign backlog (raw source now `archive/plans/fixes.txt`) and a 6-phase implementation sequence. Phases 1–6 shipped. |
| [`notification-system-expansion.md`](plans/notification-system-expansion.md) | Broaden the DB-backed notification system beyond topic requests; add the unread dot. |
| [`subject-topic-management.md`](plans/subject-topic-management.md) | Admin-editable subjects & topics. **Done 2026-09-04** — `SubjectArea` enum removed, `subject` columns are string slugs, `/admin/subjects` CRUD live. |
| [`dev-data-factory.md`](plans/dev-data-factory.md) | Dev-only page + API to spawn test data (users, classes, enrollments, topic requests) on demand without re-seeding. |

## `reviews/`

| File | Preview |
|---|---|
| [`reviews.md`](reviews/reviews.md) | **Merged** consolidation of six review docs (newest first): Design Review, Back-Navigation Review, Repeated-Markup Scan, Component Reuse & Duplication (2026-09-10); Clean-Code Review, Security Review (2026-09-09). Each section is the original verbatim. |

## `backlog/`

| File | Preview |
|---|---|
| [`TODO.txt`](backlog/TODO.txt) | Flat UI/UX + feature backlog — the source list the archived todo-cleanup sprint chunked up. Finished items are deleted, not annotated (Changes.md is the record). |
| [`TOTEST.txt`](backlog/TOTEST.txt) | Ad-hoc manual-test scratchpad. Every non-docs change adds a `[ ]` line here; tick `[Good]` or note findings when verified. |

## `reports/`

`feature-status-and-sprint-report.xlsx` — current sprint / feature status spreadsheet. Older dated copies are in `archive/reports/`.

## `to-submit/` — course deliverables

| File | Preview |
|---|---|
| [`m01-presentation/assets/DFD.md`](to-submit/m01-presentation/assets/DFD.md) | Data Flow Diagrams (Level 0 & Level 1) for the IT 124 Module 01 panel — reflect the system as actually built, every process mapped to a real portal area + API route group. |

Plus the module submission `.docx` / `.pdf` / `.pptx` files and rendered DFD images.

## `archive/`

Kept for history; not part of the living dev docs.

**`archive/plans/`**

| File | Preview |
|---|---|
| [`ui-redesign-tailwind-port.md`](archive/plans/ui-redesign-tailwind-port.md) | **Done** — ported the `design/` direction-E card system app-wide (phases 1–6, 2026-09-01). |
| [`todo-cleanup-sprint.md`](archive/plans/todo-cleanup-sprint.md) | **Done** — `backlog/TODO.txt` reorganised into 13 PR-sized chunks; chunks 1–13 + "Aug 30" batch shipped, four migrations applied. Manual checklist alongside in [`todo-cleanup-sprint.totest.txt`](archive/plans/todo-cleanup-sprint.totest.txt). |
| [`topic-requests-v2.md`](archive/plans/topic-requests-v2.md) | **Done 2026-09-03** — Topic Requests lifecycle overhaul: public/directed requests, tutor accept-to-class, DB-backed notifications, admin moderation. |
| [`help-and-faq-pages.md`](archive/plans/help-and-faq-pages.md) | **Done** (Changes.md Part 26) — role-scoped in-app Help Center per portal: quick links, walkthroughs, searchable FAQ. |
| [`pre-test-post-test-plan.md`](archive/plans/pre-test-post-test-plan.md) | **Done** (Changes.md Part 29) — learner per-session pre/post-tests. Also folded into `plans/assessments.md`. |
| [`sept-5-fixes.md`](archive/plans/sept-5-fixes.md) | **Done** (Changes.md Part 31) — a 9-item bug/UX batch from the Sept 5 review. |
| `fixes.txt`, `admin-portal-fixes.txt`, `leaner-portal-fixes.txt` | Raw, unordered backlog fragments now tracked through `plans/fixes.md`. |

**`archive/reports/`** — `feature-status-and-sprint-report-2026-09-02.xlsx`, `-2026-09-06.xlsx` (superseded by the undated copy in `reports/`).
